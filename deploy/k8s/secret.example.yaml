# Example only — do NOT commit a real secret. Create it out of band instead:
#
#   kubectl -n seanime create secret generic seanime-oidc \
#     --from-literal=client-secret='<the OIDC client secret>'
#
# or source it from External Secrets Operator / Sealed Secrets / your cloud's
# secret manager. This file exists to document the key name the Deployment expects.
apiVersion: v1
kind: Secret
metadata:
  name: seanime-oidc
  namespace: seanime
  labels:
    app.kubernetes.io/name: seanime
type: Opaque
stringData:
  client-secret: "replace-me"
